import styled from 'styled-components';
import { Container as CustomContainer } from 'src/components/shapes';

export const Container = styled(CustomContainer)``;

export const Title = styled.h1`
  font-size: 36px;
  line-height: 42px;

  max-width: 466px;

  text-align: center;
`;
